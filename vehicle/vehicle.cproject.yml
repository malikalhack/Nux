# Nux vehicle firmware (the RC car): brings up the STM32F103C8, the scheduler
# and the radio, and drives motor, servo, lights and telemetry.
project:

  layers:
    - layer: ../common/nux_common.clayer.yml

  define:
    - __RTE__

  processor:
    fpu: off
    endian: little

  add-path:
    - bsp/inc
    - RTE/Device/STM32F103C8

  output:
    type:
      - elf
      - hex
      - map

  misc:
    - for-compiler: AC6
      C:
        - -std=c11
    - for-compiler: GCC
      C:
        - -std=c11

  groups:
    - group: BSP
      add-path:
        - bsp/inc
      files:
        - file: bsp/src/bsp.c
        - file: bsp/src/handlers.c
        - file: bsp/src/startup_stm32f103xb_ac6.S
          for-compiler: AC6
        - file: bsp/src/startup_stm32f103xb_gcc.S
          for-compiler: GCC
        - file: bsp/src/syscalls.c
          for-compiler: GCC
    - group: App
      files:
        - file: src/main.c
        - file: src/debug.c
        - file: src/steering.c
